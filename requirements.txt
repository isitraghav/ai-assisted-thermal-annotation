# Core dependencies for all project scripts
# Install with: pip install -r requirements.txt

# GUI (annotation tool)
PyQt5>=5.15

# Geospatial
geopandas>=0.14
shapely>=2.0
rasterio>=1.3
pyproj>=3.6
pyogrio>=0.7

# Numerical / image
numpy>=1.26
Pillow>=10.0
piexif>=1.1

# YOLO / ML (detect_anomalies.py, yolo_train.py)
ultralytics>=8.0

# Utilities
tqdm>=4.0
